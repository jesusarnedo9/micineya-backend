spring.application.name=micine

# Configuración de conexión a la base de datos
spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5432/micine}
spring.datasource.username=${DB_USER:cinefila_user}
spring.datasource.password=${DB_PASSWORD:cinefila_password}
spring.datasource.driver-class-name=org.postgresql.Driver

# Configuración de Hibernate (el ORM que maneja SQL por vos)
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
jwt.secret=MWYVgZO6E9UH3DZkon88oCFGncfoGP579kCtYSznm/I=
jwt.expiration-ms=86400000

# Configuración de TMDB
tmdb.api.url=https://api.themoviedb.org/3
tmdb.api.key=${TMDB_API_KEY}